mistify.infer.Else
==================

.. currentmodule:: mistify.infer

.. autoclass:: Else

   
   .. automethod:: __init__

   
   .. rubric:: Methods

   .. autosummary::
   
      ~Else.__init__
      ~Else.add_module
      ~Else.apply
      ~Else.bfloat16
      ~Else.buffers
      ~Else.children
      ~Else.cpu
      ~Else.cuda
      ~Else.double
      ~Else.eval
      ~Else.extra_repr
      ~Else.float
      ~Else.forward
      ~Else.get_buffer
      ~Else.get_extra_state
      ~Else.get_parameter
      ~Else.get_submodule
      ~Else.half
      ~Else.load_state_dict
      ~Else.modules
      ~Else.named_buffers
      ~Else.named_children
      ~Else.named_modules
      ~Else.named_parameters
      ~Else.parameters
      ~Else.register_backward_hook
      ~Else.register_buffer
      ~Else.register_forward_hook
      ~Else.register_forward_pre_hook
      ~Else.register_full_backward_hook
      ~Else.register_module
      ~Else.register_parameter
      ~Else.requires_grad_
      ~Else.set_extra_state
      ~Else.share_memory
      ~Else.state_dict
      ~Else.to
      ~Else.to_empty
      ~Else.train
      ~Else.type
      ~Else.xpu
      ~Else.zero_grad
   
   

   
   
   .. rubric:: Attributes

   .. autosummary::
   
      ~Else.F
      ~Else.T_destination
      ~Else.dump_patches
      ~Else.training
   
   