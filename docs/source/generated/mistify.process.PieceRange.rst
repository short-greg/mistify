mistify.process.PieceRange
==========================

.. currentmodule:: mistify.process

.. autoclass:: PieceRange

   
   .. automethod:: __init__

   
   .. rubric:: Methods

   .. autosummary::
   
      ~PieceRange.__init__
      ~PieceRange.add_module
      ~PieceRange.apply
      ~PieceRange.bfloat16
      ~PieceRange.buffers
      ~PieceRange.children
      ~PieceRange.cpu
      ~PieceRange.cuda
      ~PieceRange.double
      ~PieceRange.eval
      ~PieceRange.extra_repr
      ~PieceRange.float
      ~PieceRange.forward
      ~PieceRange.get_buffer
      ~PieceRange.get_extra_state
      ~PieceRange.get_parameter
      ~PieceRange.get_submodule
      ~PieceRange.half
      ~PieceRange.linspace
      ~PieceRange.load_state_dict
      ~PieceRange.modules
      ~PieceRange.named_buffers
      ~PieceRange.named_children
      ~PieceRange.named_modules
      ~PieceRange.named_parameters
      ~PieceRange.parameters
      ~PieceRange.pieces
      ~PieceRange.register_backward_hook
      ~PieceRange.register_buffer
      ~PieceRange.register_forward_hook
      ~PieceRange.register_forward_pre_hook
      ~PieceRange.register_full_backward_hook
      ~PieceRange.register_module
      ~PieceRange.register_parameter
      ~PieceRange.requires_grad_
      ~PieceRange.set_extra_state
      ~PieceRange.share_memory
      ~PieceRange.state_dict
      ~PieceRange.to
      ~PieceRange.to_empty
      ~PieceRange.train
      ~PieceRange.type
      ~PieceRange.xpu
      ~PieceRange.zero_grad
   
   

   
   
   .. rubric:: Attributes

   .. autosummary::
   
      ~PieceRange.T_destination
      ~PieceRange.dump_patches
      ~PieceRange.lower
      ~PieceRange.n_pieces
      ~PieceRange.upper
      ~PieceRange.training
   
   